% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ridgeRegPrecomp.R
\name{ridgeRegPrecomp}
\alias{ridgeRegPrecomp}
\title{Individually Penalized Ridge Regression with Precomputed Matrices}
\usage{
ridgeRegPrecomp(x, y, gam, penalty.factor = rep(1, length(gam)), precomp)
}
\arguments{
\item{x}{Centered predictor matrix (n x p)}

\item{y}{Centered response vector (n x 1)}

\item{gam}{Ridge penalty parameters vector (p x 1) where gam_j = 1/nu_j}

\item{penalty.factor}{Adaptive penalty factors (p x 1), default = 1 for RSO}

\item{precomp}{List containing precomputed values:
\itemize{
\item xtx: X'X matrix (p x p)
\item xty: X'y vector (p x 1)
}}
}
\value{
Sum of squared residuals (SSR)
}
\description{
Computes the sum of squared residuals (SSR) for individually penalized
ridge regression using precomputed X'X and X'y matrices.
This is optimized for repeated calls
with the same x matrix but different gam parameters (e.g., during grid search).
}
\examples{
\dontrun{
n <- 100; p <- 10
x <- matrix(rnorm(n*p), n, p)
x <- scale(x, scale = FALSE)
y <- rnorm(n)
y <- y - mean(y)
precomp <- list(xtx = crossprod(x), xty = crossprod(x,y))
gam <- runif(p)
result <- ridgeRegPrecomp(x, y, gam, rep(1, p), precomp)
}

}
